# Product plane

One directory per project/domain, seeded by pass 1 (the Seed guard of the full path) using
`templates/project-seed.md`. Atoms live here at the lowest sufficient level;
cross-project value goes up through `_staging/` and the gatekeeper, never by
copy.
